---
description: 작업 플랜 문서 양식·구조(수정 요약·위험·기술 스택·개발 Step; 사용자·AI용)
alwaysApply: false
---

## 1. 수정 작업 요약 (사용자용)

> 이 블록만 읽어도 **무엇을, 어떻게 바꿀지**가 드러나야 한다.

### 한 줄 요약

비개발자도 읽고 **무엇을 어떻게 바꿀지** 이해할 수 있게, 아래 두 줄로 적는다.

- **현재**: 지금 동작·로직 방식
- **변경 후**: 바꾼 뒤의 동작·로직 방식

### 수정 상세(파일 단위)

1. **수정 파일**: `[파일 경로]`
   - **작업 유형**: 신규 / 수정 / 삭제
   - **요약**: `[한 줄 요약]`
2. **수정 파일**: `[파일 경로]`
   - **작업 유형**: 신규 / 수정 / 삭제
   - **요약**: `[한 줄 요약]`

---

## 2. 위험 요소 (AI용)

> 구현에 들어가기 **전에** 리스크를 짚고, 완화·대응 방법을 적어 둔다.

1. **주의 파일**: `[파일 경로]`
   - **위험 요소**:
   - **완화·대응**:
2. **주의 파일**: `[파일 경로]`
   - **위험 요소**:
   - **완화·대응**:

---

## 3. 사용 기술 스택 (AI용)

> 플랜에 쓸 **기술과 버전**은 **`.cursor/rules/02.dev-gd.mdc`**의 **`## 1. 기술 스택`**을 참고해 명시한다(정본·버전 출처는 그 절에 따른다).
> **신규 라이브러리·도구**를 도입하는 경우, 같은 파일의 **`## 1. 기술 스택`**을 **최신화**한다(`rules.mdc`·`package.json`/`requirements.txt` 등 정본과 일치하도록).

1. **프론트엔드**
   -
   -
2. **백엔드**
   -
   -
3. **라이브러리**
   -
   -
4. **기타 도구**
   -
   -

---

## 4. 개발 가이드 (AI용)

> **백엔드 → 프론트엔드** 순으로 진행한다.  
> AI가 그대로 따라 구현할 수 있도록 **충분히 구체적으로** 쓴다.

### Step 1. [짧은 제목]

- **수정 파일**: `path/to/file`
- **유형**: [NEW] / [MODIFY] / [DELETE]
- **수정 내용**:
  - (구체적 작업 bullet)

### Step 2. …

(같은 형식으로 반복)

### Step N. …

---

## 5. 개발 진행 (AI용)

> **4. 개발 가이드**의 Step을 따라 구현하고, 검증·도구 사용·코딩 관례는 **`.cursor/rules/02.dev-gd.mdc`**를 정본으로 따른다.