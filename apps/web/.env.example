# apps/web/.env.local 로 복사 후 값 채우기
# Connect → Session pooler URI (호스트에 pooler.supabase.com 포함)
# 예: postgresql://postgres.[REF]:[PASSWORD]@aws-...pooler.supabase.com:5432/postgres
DATABASE_URL=

# Vercel Cron(/api/cron/keepalive) 보호용. 프로덕션·프리뷰에 동일 값 등록
CRON_SECRET=
