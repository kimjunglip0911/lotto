# TOP8 고정 미세조정 플랜 — 실행 결과 보고

플랜 파일: `~/.cursor/plans/top8_고정_미세조정_고도화_b09eaccc.plan.md`  
실행일: 2026-03-19 (보완 실행)  
검증 구간: 회차 1164~1215 (52회), TOP_N=8·28세트 고정.

---

## 1. 이전에 끝까지 안 한 이유 (정리)

- **오해**: “테스트까지”를 **pytest 단위 테스트**로만 해석해, 플랜 §6의 **민감도·그리드·재튜닝·DB 재생성**까지 한 번에 수행하지 않음.
- **비용**: Phase 0→3 전체는 수백~수천 번의 52회×DB 재생성이라 **시간이 매우 김**; 당시 대화에서는 **코드 보강(튜너·ge3·문서·단위 테스트)** 위주로 마감됨.
- **책임**: “끝까지 하고 결과물” 요청에 **실행 범위를 명시적으로 맞추지 못함** → 이번에 §6 중 **당장 자동화 가능한 구간**을 실제 실행함.

---

## 2. 이번에 실제로 수행한 항목 (플랜 §6 대응)

| 플랜 Step | 내용 | 결과 |
|-----------|------|------|
| §6-2 | `DEFAULTS` 기준 Top8 이론 상한 (`compute_top8_coverage_metrics`) | **ge3=14**, ge4=3, ge5=0, ge6=0 |
| §6-4 | 필터 4분면 (`apply_sum_filter` × `apply_odd_even_filter` 4조합, 각각 전체 `_evaluate_params`) | **4조합 모두** 당첨회차=14, 가중점수=23, ge3=14, ge4=3 (**차이 없음**) |
| §6-3 | `run_jl3_sensitivity --perturb 0.20` (코어 5축 독립 ±20%) | 아래 표 참고 |
| §6-6 (일부) | `python -m scripts.run_jl3_52` 로 **기법52_JL3** 재생성 + 분석 | 당첨 발생 회차 **14회** (합격 미달, exit code 1은 정상) |

**아직 하지 않은 것 (플랜상 선택·고비용)**

- Phase 0→3 또는 A→B→C **전 그리드 재실행** (수시간 가능).
- §6-6의 “튜닝 베스트를 jl_service3에 반영” — 이번 민감도에서 **현재 기본값이 축별로 이미 우수**하여 **숫자 변경 없음** (아래).

---

## 3. 민감도 분석 요약 (±20%, 코어 5개 독립)

기준선(DEFAULTS와 동일 계열): **당첨 회차 14**, 가중점수 **23**.

| 파라미터 | 관찰 |
|----------|------|
| `freq_below_avg_scale` | 기본 **2.07**가 최선(14회). ±20%는 7~13회로 **악화**. |
| `freq_above_avg_scale` | 기본 **0.52**와 +20%(0.624) **동률 14회**. 기본 유지 권장. |
| `streak_near_max_penalty` | 기본 **0.45**가 14회로 최대. ±20%는 12~13회. |
| `gap_above_avg_bonus_scale` | 기본 **1.15**가 14회. ±20%는 **7회**로 크게 악화. |
| `gap_below_avg_penalty_scale` | 기본 **0.78**가 14회. ±20%는 6~9회로 악화. |

**결론**: 별도 수치 변경 없이 **현재 `jl_service3` / `run_jl3_auto_tune` DEFAULTS 코어 5개를 유지**하는 것이 이번 민감도 범위에서는 타당함.

---

## 4. 필터 4분면 결과

| sum_filter | odd_even_filter | 당첨 회차 | 점수 | ge3 |
|------------|-----------------|----------|------|-----|
| True | True | 14 | 23 | 14 |
| True | False | 14 | 23 | 14 |
| False | True | 14 | 23 | 14 |
| False | False | 14 | 23 | 14 |

**결론**: 해당 52회·현재 상수에서는 필터 ON/OFF가 **성능 지표에 영향 없음** (정렬상 기본 True/True 유지).

---

## 5. 기법52_JL3 재생성 후 검증

- `python -m scripts.run_jl3_52` 실행 후 집계: **당첨 발생 회차 14회**, 4등 발생 회차 3, 5등 발생 회차 14, 1~3등 발생 회차 0.
- Top8 이론 상한 **ge3=14**와 **실제 당첨 회차 14**가 일치 → **28세트 조합으로 5등+ 가능한 회차는 모두 당첨 처리된 상태**에 가깝음.

---

## 6. 단위 테스트

- `python -m pytest tests/test_jl3_tune.py -v` — 튜닝 정렬·`jl3_params_for_top8` 검증 (DB 불필요).  
  (CI/로컬에서 수시 실행 권장.)

---

## 7. 사용자에게 한 줄 요약

**플랜 중 “진단 + 필터 4분면 + 민감도 + 기법52_JL3 재검증”은 이번에 실행했고, 그 결과 현재 수치가 ±20%·필터 4분면 안에서는 이미 상한(14회)에 맞춰 있어 추가 숫자 수정은 불필요했습니다.**  
더 올리려면 플랜대로 **Phase 0~3 전체 재탐색** 또는 **다른 52구간 검증** 등 고비용 단계가 필요합니다.
